{{#if isLoop}}
  Loop over table
{{else if isDraw}}
  Draw {{mark}} with
  {{#each displayableAttrs as |attr|}}
    {{variable-definition variable=attr}}
  {{/each}}
{{else}}
  Adjust
  {{#each displayableAttrs as |attr|}}
    {{variable-definition variable=attr}}
  {{/each}}
{{/if}}

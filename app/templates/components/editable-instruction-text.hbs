{{#if isLoop}}
  Loop over table
{{else if isDraw}}
  Draw a {{mark}} with
  {{#each displayableAttrs as |attr|}}
    {{instruction-attr-item instruction=instruction attr=attr}}
  {{/each}}
{{else}}
  Adjust {{attrsText}}
{{/if}}

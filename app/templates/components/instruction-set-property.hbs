the
{{view "select"
  content=properties
  value=selectedProperty}}
to
{{view "select"
  content=propertyValues
  value=selectedDataItem
  optionLabelPath="content.name"
}}

{{#if isCustomValue}}
  to {{input value=customDataValue}}
{{/if}}
